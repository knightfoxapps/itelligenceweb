import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Belize",
  description: "Technical talent, English without trade-off, built for stability.",
};

export default function BelizePage() {
  return (
    <main>
      <h1>Belize</h1>
    </main>
  );
}
