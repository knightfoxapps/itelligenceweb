import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "CX Lifecycle Delivery",
  description: "Engage every customer with delivery that optimizes every call, every chat, every signal.",
};

export default function CXLifecycleDeliveryPage() {
  return (
    <main>
      <h1>CX Lifecycle Delivery</h1>
    </main>
  );
}
