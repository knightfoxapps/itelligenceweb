import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Operational Design",
  description: "Channel strategy, integration architecture, automation deployment, and AI adoption that transforms CX operations.",
};

export default function OperationalDesignPage() {
  return (
    <main>
      <h1>Operational Design</h1>
    </main>
  );
}
