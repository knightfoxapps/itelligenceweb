import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Insight Activation",
  description: "The trends behind customer behavior surfaced from every interaction.",
};

export default function InsightActivationPage() {
  return (
    <main>
      <h1>Insight Activation</h1>
    </main>
  );
}
